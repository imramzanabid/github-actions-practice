name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello from GitHub Actions!"

      - name: Mera Naam
        run: echo "Ye Ramzan ka pehla workflow hai!"